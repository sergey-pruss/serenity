# Cloudflare Workers ASSETS (wrangler.jsonc directory: ".")
# Видео и тяжёлые файлы — только static origin; лимит Worker 25 MiB/файл.
*.mp4

# Локальный symlink _sa → . (dev); иначе _sa/_sa/… и весь репо в индексе.
_sa
_sa/

node_modules/
.git/
tmp/
.cursor/
.continue/
