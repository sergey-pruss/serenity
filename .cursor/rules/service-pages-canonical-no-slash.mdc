---
description: Канон URL страниц услуг на статике — без завершающего слэша
globs: targeting/**,kontekstnaya_reklama/**,nginx/serenity-router.live.conf,nginx/static.serenity.agency.live.conf,sitemap.xml,html/partials/section-services.html
---

# URL услуг без слэша

Страницы на статическом контуре без слэша в каноне: листинг **`/services`**, **`/targeting`**, **`/kontekstnaya_reklama`** (и будущие по тому же шаблону):

- **Канон** — **без** завершающего `/` (как legacy/Nuxt и `sitemap.xml`).
- **canonical**, **og:url**, **`<loc>` в sitemap**, **`href` внутренних ссылок**, **`urlPath` в `json/services/*/service.config.json`** — один и тот же путь без слэша.
- **Nginx** (`serenity-router.live.conf`, `static.serenity.agency.live.conf`): `location = /path` → `try_files /path/index.html`; `location = /path/` → **301** на `/path` (для `/services`, `/targeting`, `/kontekstnaya_reklama`). Подстраницы `/services/seo` и т.п. — legacy, не путать с листингом `/services`.
- **Не путать** с блогом: листинг **`/blog/`** — канон **со** слэшем (отдельные правила в vhost).

Проверки: `npm run test:service-page-urls`, `npm run test:routing-config`. После выкладки vhost: `ORIGIN=https://serenity.agency npm run test:service-page-urls`.

Полный регламент: [`AGENTS.md`](../AGENTS.md).
