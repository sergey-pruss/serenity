node_modules/
playwright-report/
test-results/
compare/*
!compare/form-mobile-blend-50.png
!compare/form-mobile-side-by-side.png
.DS_Store
# Cursor: версионируем только общие правила в `.cursor/rules/*.mdc`;
# локальные служебные файлы (логи отладки и т.п.) не коммитим.
.cursor/*
!.cursor/rules/
.cursor/rules/*
!.cursor/rules/*.mdc
# Шаблон MCP для Cursor (секреты — только в локальном `.cursor/mcp.json`, см. пример).
!.cursor/mcp.json.example
.claude/
dist/
.astro/
standalone/
public/img/
tmp/
# Локальный симлинк `/_sa/` → корень репозитория (удобство dev); в git и rsync не кладём.
_sa
artifacts/
# Локальные снимки миграции / baseline (не коммитим повторно)
.snapshots/
.wrangler/

# локальные черновики / снимки вне пайплайна
_patch_ribbon.py
_strip_bad_ribbon.py
_serenity_home.html

publish/index.html
publish/svgset.svg
